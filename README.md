# TinyGPT

Minimal Transformer language model in one notebook.

- `tinygpt.ipynb` defines a small GPT style model in TensorFlow, including tokenizer, Transformer blocks, training loop, and text generation.
